---
description: Pre-flight checks then deploy (env validation, migrations, bundle size, ship audit, health check)
---

Invoke the ultraship:deploy skill to run pre-flight checks and deploy this project. Validate environment variables, check migrations, audit bundle size, run the full /ship scorecard, deploy, then health check the production URL.
