---
description: Safety guardrails — blocks destructive commands (rm -rf, DROP TABLE, force-push) and optionally restricts edits to a directory
---

Invoke the ultraship:guard skill to activate safety guardrails. Blocks destructive Bash commands and optionally restricts file edits to a specific directory. Every blocked action explains what was caught and why.